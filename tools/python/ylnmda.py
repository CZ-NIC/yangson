"""This script reads the NMDA-aware version of YANG library [RFC8525]_
and generates a corresponding old-style YANG library file that can be
used with Yangson.

Optionally, it can also download missing modules using the
``location`` parameter (if available).

"""

import json
import os
import sys

def main():
    print("TBD")
    return 0


if __name__ == '__main__':
    sys.exit(main())
